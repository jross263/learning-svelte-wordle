import { describe, expect, it } from 'vitest';
import { render, screen } from '@testing-library/svelte';
//import GameTile from '../src/components/GameTile.svelte';

describe("GameRow.svelte", () => {
  it("should render 5 GameTiles", () => {
    
  });
})